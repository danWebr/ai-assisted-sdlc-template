# Claude Code project instructions

@AGENTS.md

## Claude Code compatibility

`AGENTS.md` is canonical. The entries under `.claude/skills/` are thin adapters that load the corresponding canonical project skill from `.agents/skills/`; do not copy workflow content into this file or those adapters.
