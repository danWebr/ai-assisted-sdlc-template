# Template maintainer onboarding

This document belongs only to the canonical template source. A generated project removes it during `mise run bootstrap`.

Keep the canonical source at `danWebr/ai-assisted-sdlc-template`, preserve the project markers in `README.md`, `AGENTS.md`, and `CONTEXT.md`, and exercise bootstrap behavior only in a disposable extracted repository. Never bootstrap the canonical source itself.

Bootstrap is intentionally limited to repository mechanics. Product, domain, stack, architecture, publication, credentials, and infrastructure belong to later human-led workflow stages.
